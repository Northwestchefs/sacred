# English Bible Importer (JPS 1917)

Imports English source data from `reference/english-bible/raw/` and emits normalized book-level JSON files compatible with Sacred's reader reference shape.

Run:

```bash
node scripts/english-bible/importer/index.js
```

Dry run:

```bash
node scripts/english-bible/importer/index.js --dry-run
```
